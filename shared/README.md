# Shared Module

Shared types, utilities, and constants across frontend and backend.

## Structure

- `types/` - TypeScript/Python type definitions
- Shared constants and enums

## Usage

This module contains code shared between frontend and backend to ensure type consistency.

